# EZ-ADMIN-SPRINGBOOT4

![Version](https://img.shields.io/badge/version-0.0.1--blue)
![License](https://img.shields.io/badge/license-MIT-blue)
![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-4.0-brightgreen)

> 一款基于 Spring Boot 4.x + JDK 21 的现代化轻量级后台管理系统

## 📖 项目简介

EZ-ADMIN 是一款简单易用、代码整洁的 RBAC 后台管理系统。采用现代化的技术栈，没有复杂的过度封装，逻辑清晰，可轻松上手，易于二次开发。

**设计理念**：
- ✅ **零模块架构**：三层分离（API 层、业务聚合层、原子服务层）
- ✅ **代码优先**：减少配置，专注于业务逻辑
- ✅ **性能至上**：Redis 两层缓存架构 + 批量操作优化
- ✅ **规范统一**：严格的代码规范和完善的文档

**适用场景**：
- 学习 Spring Boot 4.x + JDK 21 最新技术栈
- 作为项目脚手架快速搭建后台系统
- 中小型企业管理系统
- 学习参考和二次开发

---

## 🎯 核心特性

### 技术亮点
- 🚀 **最新技术栈**：Spring Boot 4.0 + JDK 21 + PostgreSQL
- 🔐 **完善权限体系**：Sa-Token + JWT + Redis 缓存
- ⚡ **高性能缓存**：两层缓存架构（用户→角色→权限），避免缓存雪崩
- 📦 **批量操作优化**：MyBatis-Plus JDBC Batch，性能提升 20-100 倍
- 🎨 **代码生成器**：一键生成 Entity、Mapper、Service
- 📚 **完善文档**：详细的开发规范和技术文档

### 系统功能
- 👤 **用户管理**：用户信息的增删改查、状态管理、角色分配
- 🎭 **角色管理**：角色权限分配、数据范围控制、菜单权限配置
- 🏢 **部门管理**：组织架构管理、树形结构展示
- 📋 **菜单管理**：动态菜单配置、路由权限、按钮权限
- 📖 **字典管理**：系统字典配置、数据标准化
- 🔑 **权限控制**：基于 RBAC 的访问控制、细粒度权限管理
- 📊 **操作日志**：用户操作行为记录和审计

---

## 🛠 技术选型

| 技术领域 | 技术选型 | 版本 | 说明 |
|---------|---------|------|------|
| **核心框架** | Spring Boot | 4.0.0 | 最新版 Spring Boot 框架 |
| **开发语言** | Java | 21 | LTS 版本，虚拟线程支持 |
| **ORM 框架** | MyBatis-Plus | 3.5.15 | 强大的 MyBatis 增强工具 |
| **数据库** | PostgreSQL | 15+ | 开源关系型数据库 |
| **缓存** | Redis | 7.0+ | 高性能键值存储 |
| **权限认证** | Sa-Token | 1.44.0 | 轻量级权限认证框架 |
| **对象映射** | MapStruct | 1.6.3 | 编译期对象映射工具 |
| **工具库** | Lombok | 1.18.30 | 简化 Java 代码 |
| **API 文档** | SpringDoc OpenAPI | 3.0.0 | 自动生成 API 文档 |
| **构建工具** | Maven | 3.9+ | 项目构建和依赖管理 |

---

## 🏗 系统架构

### 分层设计

```
ez-admin-springboot4/
├── api/                # 接口层（手动编写）
│   └── system/         # 系统管理接口
├── service/            # 业务聚合层（手动编写）
│   ├── user/           # 用户业务
│   ├── role/           # 角色业务
│   ├── menu/           # 菜单业务
│   └── permission/     # 权限管理
├── modules/system/     # 原子服务层（代码生成）
│   ├── entity/         # 实体类
│   ├── mapper/         # 数据访问层
│   └── service/        # 原子服务
├── dto/                # 数据传输对象（手动编写）
│   └── {module}/
│       ├── req/        # 请求对象
│       └── vo/         # 响应对象
└── common/             # 通用模块
    ├── cache/          # 缓存封装
    ├── component/      # 组件配置
    ├── constant/       # 常量定义
    ├── exception/      # 异常处理
    ├── mapstruct/      # 对象映射
    ├── model/          # 通用模型
    ├── permission/     # 权限实现
    ├── redis/          # Redis 配置
    └── web/            # Web 配置
```

### 架构优势

| 层级 | 职责 | 优势 |
|------|------|------|
| **API 层** | 接收请求、参数校验、返回响应 | 轻薄、无业务逻辑 |
| **业务聚合层** | 组合原子服务、实现复杂业务 | 业务逻辑清晰、可复用 |
| **原子服务层** | 单表 CRUD、无业务逻辑 | 代码生成、减少重复 |

---

## ⚡ 性能优化

### 1. 两层缓存架构

**设计理念**：缓存模型跟随权限模型（RBAC），避免缓存雪崩

```
用户ID → 角色标识列表 → 菜单权限列表
  ↓         ↓               ↓
[1天]     [30天]         [30天]
```

**优势**：
- ✅ 修改角色权限只需清除一层缓存
- ✅ 避免 10 万用户同时清除缓存导致的缓存雪崩
- ✅ 缓存降级回填，自动从数据库加载

### 2. 批量操作优化

**优化前**（反模式）：
```java
// ❌ N 次网络 IO
relations.forEach(mapper::insert);
```

**优化后**（批量插入）：
```java
// ✅ 1 次网络 IO
roleMenuRelationService.saveBatch(relations);
```

**性能提升**：20-100 倍（取决于数据量）

### 3. 路由权限缓存

- 启动时预加载所有路由权限规则到 Redis
- 请求时从缓存读取，零数据库访问
- 支持 HTTP Method + Path 精确匹配

---

## 🚀 快速开始

### 环境要求

| 工具/环境 | 版本要求 | 说明 |
|----------|---------|------|
| JDK | 21+ | Java 开发工具包 |
| Maven | 3.9+ | 项目构建工具 |
| PostgreSQL | 15+ | 数据库服务器 |
| Redis | 6.0+ | 缓存服务器 |
| IDE | IntelliJ IDEA | 推荐开发工具 |

### 数据库初始化

1. **创建数据库**
```sql
CREATE DATABASE ez_admin
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_general_ci;
```

2. **执行初始化脚本**
```bash
# 方式 1：命令行
psql -U postgres -d ez_admin -f doc/sql/ez-admin-postgres.sql

# 方式 2：数据库工具
# 在 pgAdmin 或 DBeaver 中执行 doc/sql/ 目录下的 SQL 文件
```

### 配置环境变量

**方式 1：IDE 配置**（推荐开发环境）

在 IntelliJ IDEA 中配置运行环境变量：
```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ez_admin
DB_USERNAME=postgres
DB_PASSWORD=postgres
REDIS_HOST=localhost
REDIS_PORT=6379
SA_TOKEN_JWT_SECRET=your-secret-key
```

**方式 2：.env 文件**

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件，填入实际值
```

**方式 3：系统环境变量**

```bash
# Windows (PowerShell)
$env:SA_TOKEN_JWT_SECRET="your-secret-key"

# Linux/Mac
export SA_TOKEN_JWT_SECRET="your-secret-key"
```

> 💡 **详细配置指南**：查看 [doc/environment-config-guide.md](doc/environment-config-guide.md)

### 启动项目

```bash
# 1. 克隆项目
git clone https://github.com/Caoshenyang/ez-admin-springboot4.git
cd ez-admin-springboot4

# 2. 编译项目
mvn clean compile

# 3. 启动项目
mvn spring-boot:run
```

### 访问系统

项目启动后访问：

| 访问地址 | 说明 |
|---------|------|
| http://localhost:8080 | 应用地址 |
| http://localhost:8080/swagger-ui.html | API 文档 |
| http://localhost:8080/doc.html | Knife4j 文档 |

**默认账号**：
- 用户名：`admin`
- 密码：`admin123`

---

## 📁 项目结构

```
ez-admin-springboot4/
├── doc/                              # 文档目录
│   ├── sql/                          # 数据库脚本
│   │   ├── ez-admin-postgres.sql    # 数据库初始化
│   │   ├── menu-init-data.sql        # 菜单初始化数据
│   │   └── route-permission-init-data.sql  # 路由权限初始化
│   ├── batch-insert-optimization.md  # 批量插入优化文档
│   ├── environment-config-guide.md   # 环境变量配置指南
│   └── README.md                      # 文档索引
│
├── src/main/java/com/ez/admin/
│   ├── api/                          # 接口层（手动编写）
│   │   └── system/                   # 系统管理接口
│   │       ├── UserController.java   # 用户管理
│   │       ├── RoleController.java   # 角色管理
│   │       ├── MenuController.java   # 菜单管理
│   │       ├── DeptController.java   # 部门管理
│   │       ├── DictController.java   # 字典管理
│   │       └── SystemController.java # 系统管理
│   │
│   ├── service/                      # 业务聚合层（手动编写）
│   │   ├── user/                     # 用户业务
│   │   ├── role/                     # 角色业务
│   │   ├── menu/                     # 菜单业务
│   │   └── permission/               # 权限管理
│   │
│   ├── modules/system/               # 原子服务层（代码生成）
│   │   ├── entity/                   # 实体类
│   │   ├── mapper/                   # 数据访问层
│   │   └── service/                  # 原子服务
│   │
│   ├── dto/                          # 数据传输对象（手动编写）
│   │   ├── user/                     # 用户相关 DTO
│   │   ├── role/                     # 角色相关 DTO
│   │   ├── menu/                     # 菜单相关 DTO
│   │   └── system/                   # 系统相关 DTO
│   │
│   └── common/                       # 通用模块
│       ├── cache/                    # 缓存封装
│       ├── component/                # 组件配置
│       ├── constant/                 # 常量定义
│       ├── exception/                # 异常处理
│       ├── mapstruct/                # 对象映射
│       ├── model/                    # 通用模型
│       ├── permission/               # 权限实现
│       ├── redis/                    # Redis 配置
│       └── web/                      # Web 配置
│
└── src/main/resources/
    ├── mapper/                       # MyBatis XML 映射
    ├── application.yml               # 主配置文件
    ├── application-dev.yml           # 开发环境配置
    └── application-prod.yml          # 生产环境配置
```

---

## 🔧 核心功能

### 1. 用户管理

- 用户信息的增删改查
- 用户状态管理（启用/禁用）
- 用户角色分配
- 用户数据权限范围

### 2. 角色管理

- 角色的增删改查
- 角色权限分配（菜单权限）
- 角色数据权限配置（部门范围）
- 角色状态管理

### 3. 菜单管理

- 动态菜单配置
- 菜单树形结构展示
- 路由权限配置
- 按钮权限控制

### 4. 权限控制

**RBAC 权限模型**：
- 基于角色的访问控制
- 菜单权限（前端路由）
- 按钮权限（接口权限）
- 数据权限（部门范围）

**技术实现**：
- Sa-Token + JWT 无状态认证
- Redis 两层缓存架构
- 路由拦截式权限校验

### 5. 操作日志

- 用户操作行为记录
- 操作模块、操作类型、操作结果
- 可追溯的审计日志

---

## 📚 开发指南

### 代码规范

项目遵循严格的代码规范，详见 [CLAUDE.md](CLAUDE.md)：

| 规范 | 说明 |
|------|------|
| **命名规范** | VO 按用途区分（UserDetailVO、UserListVO、UserTreeVO） |
| **注解规范** | 所有 DTO、VO 必须使用 `@Schema` 注解 |
| **对象构造** | DTO 使用 `@Data`，VO 使用 `@Getter + @Builder` |
| **MapStruct** | 强制使用 MapStruct 进行对象转换 |
| **MyBatis-Plus** | 单表用编程式，复杂逻辑用 XML |
| **禁止事项** | 禁止使用 `var`、禁止循环数据库操作 |

### VO 命名规范

```
按用途区分：
- UserDetailVO    # 用户详情
- UserListVO      # 用户列表
- UserTreeVO      # 用户树形
- UserOptionVO    # 用户选项
```

### 对象转换

```java
// ✅ 推荐：使用 MapStruct
UserDetailVO vo = userConverter.toDetailVO(user);

// ❌ 禁止：手动 copy 或使用 BeanUtils
UserDetailVO vo = new UserDetailVO();
BeanUtils.copyProperties(user, vo);
```

### 批量操作

```java
// ✅ 推荐：使用 IService.saveBatch
roleMenuRelationService.saveBatch(relations);

// ❌ 禁止：循环插入
relations.forEach(mapper::insert);
```

---

## 🔐 权限设计

### 两层缓存架构

```
┌─────────────────────────────────────┐
│  用户登录 → StpInterface           │
│  ↓                                  │
│  getPermissionList()                │
│  ↓                                  │
│  ① 用户ID → 角色列表 [缓存 1天]     │
│  ② 角色标识 → 权限列表 [缓存 30天]  │
│  ③ 返回权限码集合                   │
└─────────────────────────────────────┘
```

**优势**：
- 缓存模型跟随 RBAC 权限模型
- 修改角色权限只需清除一层缓存
- 避免 10 万用户同时清除缓存

### 路由权限校验

```
请求 → Sa-Token 拦截器
  ↓
① 从 Redis 缓存获取路由权限规则
② 匹配当前请求路径和方法
③ 找到权限码 → 校验用户权限
④ 未找到 → 只校验登录
```

**性能**：零数据库访问，全部从 Redis 缓存读取

---

## 📖 文档导航

### 开发规范

- [CLAUDE.md](CLAUDE.md) - 项目开发规范（必读）
- [doc/README.md](doc/README.md) - 文档索引
- [doc/vo-naming-spec.md](doc/vo-naming-spec.md) - VO 命名规范
- [doc/annotation-spec.md](doc/annotation-spec.md) - @Schema 注解规范
- [doc/object-construction-spec.md](doc/object-construction-spec.md) - 对象构造规范

### 技术文档

- [doc/mapstruct-guide.md](doc/mapstruct-guide.md) - MapStruct 使用指南
- [doc/mybatis-plus-guide.md](doc/mybatis-plus-guide.md) - MyBatis-Plus 使用规范
- [doc/error-code-design.md](doc/error-code-design.md) - 错误码设计规范
- [doc/batch-insert-optimization.md](doc/batch-insert-optimization.md) - 批量插入优化
- [doc/environment-config-guide.md](doc/environment-config-guide.md) - 环境变量配置指南

### 功能文档

- [doc/redis-cache-optimization.md](doc/redis-cache-optimization.md) - Redis 缓存优化
- [doc/super-admin-permission-sync.md](doc/super-admin-permission-sync.md) - 超级管理员权限同步
- [doc/route-check-migration.md](doc/route-check-migration.md) - 路由校验迁移

---

## 🧪 测试指南

### 单元测试

```bash
# 运行所有测试
mvn test

# 运行指定测试类
mvn test -Dtest=UserServiceTest
```

### 集成测试

```bash
# 跳过测试打包
mvn clean package -DskipTests
```

---

## 🐛 故障排查

### 常见问题

**1. JWT 密钥未设置**
```
错误：jwtSecretKey 不能为空
解决：export SA_TOKEN_JWT_SECRET="your-secret-key"
```

**2. 数据库连接失败**
```
检查：DB_HOST、DB_PORT、DB_USERNAME、DB_PASSWORD
```

**3. Redis 连接失败**
```
检查：REDIS_HOST、REDIS_PORT、REDIS_PASSWORD
```

**4. 权限校验失败**
```
检查：角色是否分配菜单权限
检查：Redis 缓存是否已初始化
```

---

## 🤝 贡献指南

欢迎提交 Issue 来描述你的建议或问题。

**注意**：本项目暂不接受 Pull Request。

---

## 📄 开源协议

本项目采用 [MIT](LICENSE) 许可证。

---

## 🌟 Star History

如果这个项目对你有帮助，请给个 Star 支持一下！

---

## 📮 联系方式

- 作者：ez-admin
- 邮箱：your-email@example.com
- GitHub：[Caoshenyang](https://github.com/Caoshenyang)

---

**🎉 感谢使用 EZ-ADMIN！**
