spring:
  application:
    name: ez-admin-springboot4
  datasource:
    url: jdbc:postgresql://121.5.162.159:3367/ez_admin_db?useUnicode=true&characterEncoding=UTF-8&stringtype=unspecified
    username: root
    password: yang@0916
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: true  # 显式启用 OpenEntityManagerInView（消除警告）
